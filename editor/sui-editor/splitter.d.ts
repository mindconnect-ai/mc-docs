/**
 * Wires the gutters found under {@code root}. Call once after the layout HTML
 * is in place. Restores any previously-saved sizes first.
 */
export declare function installSplitters(root: HTMLElement): void;
